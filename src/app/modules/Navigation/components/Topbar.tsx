import Typography from '../../../components/Typography/Typography';
import { TopbarContainer } from '../Navigation.styles';

export default function Topbar() {
  return (
    <TopbarContainer container size="sm">
      <Typography>sdf</Typography>
    </TopbarContainer>
  );
}
