export type LayoutContextValues = {
  navTopbarHeight: number;
  footerHeight: number;
  setNavTopbarHeight: React.Dispatch<React.SetStateAction<number>>;
  setFooterHeight: React.Dispatch<React.SetStateAction<number>>;
};
